# Plugins Directory

This collection includes a `plugins/` directory for shipping custom Ansible plugins, modules and module utilities. The repository currently does not include custom modules, but the directory layout is available for future extensions.

See the repository `plugins/` README for guidance on adding plugins and supported plugin types.
