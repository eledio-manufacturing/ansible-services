# common

Base system setup. Updates apt cache and installs essential packages.
Run this role first in any playbook.

## Variables

| Variable | Default | Description |
|---|---|---|
| `common_packages` | see defaults | List of apt packages to install |
