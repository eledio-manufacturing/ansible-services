galaxy_info:
  author: eledio
  description: Base system setup — apt cache update and essential package installation
  license: GPL-2.0-or-later
  min_ansible_version: "2.14"
  platforms:
    - name: Debian
      versions:
        - all  # includes Raspberry Pi OS (bookworm/bullseye)
    - name: Ubuntu
      versions:
        - all
  galaxy_tags:
    - common
    - base
    - bootstrap

dependencies: []
