# install_uv

Installs [uv](https://github.com/astral-sh/uv) Python package manager system-wide.
Skips install if binary already present.

## Variables

| Variable | Default | Description |
|---|---|---|
| `uv_install_dir` | `/usr/local/bin` | Directory to install uv binary into |
